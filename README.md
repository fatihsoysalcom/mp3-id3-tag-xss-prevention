# mp3-id3-tag-xss-prevention
This example demonstrates how Cross-Site Scripting (XSS) vulnerabilities can arise from displaying unsanitized ID3 metadata extracted from MP3 files in a web application. It simulates a malicious ID3 comment tag containing an XSS payload and shows both a vulnerable page that executes the script and a secure page that prevents it using proper HTML e
